# The complete quality pipeline. lint-staged formats and lints only the staged
# files; typecheck, coverage and build are project-wide because none of them is
# a per-file property.
npx lint-staged
npm run typecheck
npm run test:coverage
npm run build
